#include "LinkList.h"
void Del(LinkList &L){
    LNode *p = L->next;
    LNoede *pre = L;
    int data = |p->data|;
    int flag = 0;
    while(p != NULL){
        if(|pre->data| == p->data)
        
        
    }
}
int main(){
    LinkList A = List_RearInsert(A);
}